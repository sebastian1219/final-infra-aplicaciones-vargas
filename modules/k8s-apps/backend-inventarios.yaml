apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventarios-dep
  namespace: backend
  labels:
    app: inventarios
    tier: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: inventarios
      tier: backend
  template:
    metadata:
      labels:
        app: inventarios
        tier: backend
    spec:
      containers:
        - name: inventarios
          image: nginx:1.25-alpine
          ports:
            - containerPort: 80
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 15
            periodSeconds: 20
            timeoutSeconds: 2
            failureThreshold: 3
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "250m"
              memory: "256Mi"
          env:
            - name: SERVICE_NAME
              value: "inventarios"
            - name: LOG_LEVEL
              value: "info"
---
apiVersion: v1
kind: Service
metadata:
  name: inventarios-svc
  namespace: backend
  labels:
    app: inventarios
    tier: backend
spec:
  type: ClusterIP
  selector:
    app: inventarios
    tier: backend
  ports:
    - port: 8080
      targetPort: 80
      protocol: TCP
